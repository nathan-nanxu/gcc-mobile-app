import 'package:flutter/material.dart';

class GccScreen extends StatelessWidget {
  static const routeName = "/gcc";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("GCC"),
      ),
      body: Center(
        child: Text("GCC"),
      ),
    );
  }
}
