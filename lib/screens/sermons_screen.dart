import 'package:flutter/material.dart';

class SermonsScreen extends StatelessWidget {
  static const routeName = "/sermons";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Sermons"),
      ),
    );
  }
}
